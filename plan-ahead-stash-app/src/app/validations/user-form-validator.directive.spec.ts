import { UserFormValidatorDirective } from './user-form-validator.directive';

describe('UserFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
