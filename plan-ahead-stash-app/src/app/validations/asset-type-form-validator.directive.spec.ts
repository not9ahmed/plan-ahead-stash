import { AssetTypeFormValidatorDirective } from './asset-type-form-validator.directive';

describe('AssetTypeFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AssetTypeFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
